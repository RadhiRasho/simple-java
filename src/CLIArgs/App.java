package CLIArgs;

public class App {
    public static void main(String[] args) throws Exception {
        // ! Print Command Line Arguments Passed in
        // PrintCommandLineArgs.printArgs(args);

        // ! Checking for Arguments that were passed in
        // ! if none were, complain about that
        // CheckForCommandLineArgs.checkForCliArgs(args);

        // ! Check for Argument Equaling a specific value
        // CheckForHelpFlag.checkHelpFlag(args);
    }
}
